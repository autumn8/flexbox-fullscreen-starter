# Flexbox-Fullscreen-Starter

A simple fullscreen flexbox layout with custom vh implementation to correctly calculate viewport height with browser chrome.


## Setup
`npm install`

## Run
Uses vite for serving and hot reloads.

`npm run dev`


